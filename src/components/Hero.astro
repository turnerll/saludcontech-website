<section class="relative overflow-hidden">
  <!-- Subtle gradient background -->
  <div class="absolute inset-0 bg-gradient-to-br from-purple-50/80 via-white to-white"></div>
  <div class="absolute top-20 right-0 w-96 h-96 bg-purple/5 rounded-full blur-3xl"></div>
  <div class="absolute bottom-0 left-0 w-72 h-72 bg-purple/5 rounded-full blur-3xl"></div>

  <div class="relative max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-20 sm:py-28 lg:py-36">
    <div class="max-w-3xl">
      <!-- Eyebrow -->
      <div class="inline-flex items-center gap-2 px-4 py-1.5 bg-purple/10 rounded-full mb-6 fade-in">
        <div class="w-2 h-2 bg-purple rounded-full animate-pulse"></div>
        <span class="text-purple text-sm font-medium">Latino Health Tech Community</span>
      </div>

      <!-- Headline -->
      <h1 class="text-4xl sm:text-5xl lg:text-6xl font-extrabold text-navy leading-tight mb-6 fade-in">
        Build the Health Tech Your Community
        <span class="gradient-text"> Actually Needs.</span>
      </h1>

      <!-- Subhead -->
      <p class="text-lg sm:text-xl text-navy-700 leading-relaxed mb-10 max-w-2xl fade-in">
        SaludConTech is where founders, clinicians, and builders from communities of color
        come together to create health technology that works for everyone.
      </p>

      <!-- CTAs -->
      <div class="flex flex-col sm:flex-row gap-4 fade-in">
        <a href="/community" class="btn-primary text-center">
          Join Our Community
          <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
          </svg>
        </a>
        <a href="#mission" class="btn-secondary text-center">
          Learn More
        </a>
      </div>
    </div>
  </div>
</section>

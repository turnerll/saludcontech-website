# SaludConTech — Lossless Handoff 2026-08-11

## You are here

- **Project folder:** `/home/djtl/Projects/saludcontech-website`
- **GitHub repo:** `https://github.com/turnerll/saludcontech-website` (public)
- **Domain:** `saludcontech.com`
- **How to build:** `cd /home/djtl/Projects/saludcontech-website && source ~/.nvm/nvm.sh && nvm use 22 && npm run build`
- **Output:** `dist/` (5 static pages)
- **Latest commit on main:** `443ea8c` — handoff: lossless 2026-08-11 snapshot + CLAUDE.md rules

**One-line next step:** add the Cloudflare DNS CNAME for `saludcontech.com` on the Mac Mini, verify the live domain serves the new build, test a signup + PostHog event, then capture the COMPA PostHog config from the Mac Mini browser and replicate it in the Ligazon/SCT PostHog project.

## Context

Daniel wants `saludcontech.com` live with Ligazon-quality structure, personalized for SaludConTech, and COMPA-style PostHog analytics (IP, geolocation, session recording, funnels). This session consolidated two folders/repos into one canonical location.

**Reference material:** `docs/SCT-STRATEGY-CONTEXT.md` contains the full research corpus (mission, timeline, membership, monetization tiers, ecosystem strategy, COMPA PostHog checklist, execution phases, rules, content sources).

## DONE and verified

- **Canonical folder/repo set:** `/home/djtl/Projects/saludcontech-website` → `turnerll/saludcontech-website`.
- **Merged from the old `saludcontech-site`:**
  - PostHog `/ingest` proxy: `functions/ingest/[[path]].js`
  - Supabase signup wiring: `src/components/Newsletter.astro`
  - Slack community digest: `docs/slack-community-digest.md`
  - SCT facts: `data/facts.json`
- **PostHog client** in `src/layouts/Layout.astro`:
  - Project key: `phc_kXyeJqG2JKPbu3Svo2E7KkYsjZn6yFHjqxLPqBBbJ3Us`
  - `api_host: '/ingest'`, `ui_host: 'https://us.posthog.com'`
  - Session recording enabled in code, `respect_dnt: true`
  - Super props: `environment: 'production'`, `app_version: '2026.08.11'`, `site: 'saludcontech'`
  - UTM params captured as super props
  - On newsletter signup: `posthog.identify(email, ...)` + `posthog.capture('relaunch_signup', ...)`
- **Supabase signup** posts to `public.signups` in project `jcxagmhvwakkxogfyrzv` with `source: 'saludcontech.com'`, `interest: 'newsletter'`. IP/UA are captured by trigger.
- **Build passes:** `npm run build` outputs `dist/` with 5 pages.
- **Pushed:** `443ea8c` on `main`.
- **Cloudflare Pages:** project `saludcontech-relaunch` connected to `turnerll/saludcontech-website`; build config `npm run build` / `dist`.
- **Deployment live:** id `0e836df8`, URL `https://0e836df8.saludcontech-relaunch.pages.dev/` (HTTP 200 verified).

## PARTIALLY done

- Custom domain `saludcontech.com` is added to Pages but status is `pending`; DNS CNAME missing. API could not add it because the Pages token lacks `DNS:Edit`. Old 2019 page still loads at `https://saludcontech.com/`.
- PostHog dashboard-side setup not done: session-recording toggle, funnel, dashboard, signup alert.
- COMPA PostHog config capture not started.

## NOT started

- DNS CNAME for `saludcontech.com` (blocked on Mac Mini dashboard click).
- PostHog dashboard-side setup (session recording toggle, funnel, dashboard, signup alert).
- Replace hardcoded stats in `src/components/Stats.astro` with sourced facts from `data/facts.json`.
- `scripts/verify-astro.cjs` and `.github/workflows/` for CI/CD.
- Membership/pricing pages.
- Outreach engine (Luma, Salesflow, Twenty CRM).
- Full strategy context is in `docs/SCT-STRATEGY-CONTEXT.md` (research, tiers, plan, rules).

## Step-by-step: what to do next

### 1. Add the DNS CNAME (Mac Mini browser)

- On the Mac Mini, Chrome should be on Cloudflare dashboard for `saludcontech.com`.
- Go to **DNS → Records**.
- Add record:
  - Type: `CNAME`
  - Name: `@`
  - Target: `saludcontech-relaunch.pages.dev`
  - TTL: Auto
  - Proxy status: **Proxied** (orange cloud)
- Save. Wait 30–120 seconds.

**Verify:**

```bash
# Should show the new build, NOT the old 2024 LiteSpeed page
curl -I https://saludcontech.com/
# Expected: cf-cache-status, no "x-turbo-charged-by: LiteSpeed", last-modified in 2026
```

**Optional but correct:** add a redirect rule so `saludcontech.org` → `saludcontech.com` (Cloudflare dashboard → Redirect Rules or a dummy DNS + Page Rule). Do not serve duplicate content on both domains.

If still old after 2 minutes:

```bash
dig saludcontech.com CNAME +short
# Expected: saludcontech-relaunch.pages.dev
```

### 2. Test signup + PostHog end-to-end

- Open `https://saludcontech.com/` in a real browser.
- Scroll to the newsletter form, enter a test email, submit.
- **Supabase check:**

```bash
source ~/pai/secrets/ligazon_backend.env
psql "$LIGAZON_SUPABASE_POOL_URL" -c "SELECT created_at, email, source, interest FROM public.signups ORDER BY created_at DESC LIMIT 5;"
```

(If the env var name differs, inspect `~/pai/secrets/ligazon_backend.env`.)

- **PostHog check:** go to `https://us.posthog.com/project/464719/events` and look for `relaunch_signup` event with `site: saludcontech` and a city under GeoIP.

**After live verification:**

```bash
# Optional: confirm the Ligazon weekly digest still includes an SCT block
gh workflow run weekly-digest.yml --repo turnerll/ligazon
```

### 3. Capture COMPA PostHog config and replicate

Daniel must stay logged into the **COMPA** PostHog account on the Mac Mini while you capture. Save everything to:

- `docs/posthog-compa-config.md`
- `screenshots/posthog-compa/`

Capture at minimum:

1. **Project Settings → Session replay:** toggle ON/OFF, recording rules, masking settings.
2. **Project Settings → Autocapture:** enabled or disabled (COMPA uses explicit events only).
3. **Data management → Events:** list of custom events (e.g., `signup_completed`, `rsvp_confirmed`, `invite_sent`).
4. **Data management → Actions:** any actions defined.
5. **Insights → Funnels:** COMPA funnel steps (likely visit → signup → ... → confirmed).
6. **Insights → Trends/Dashboards:** any dashboard names and tiles.
7. **Feature flags / Experiments:** any active flags.
8. **Toolbar settings / Authorized URLs.**

Then have Daniel log into the **Ligazon/SCT** PostHog account (project `464719`) and apply the equivalent for SaludConTech:

- Session replay: **ON**.
- Autocapture: match COMPA (likely OFF).
- Create funnel: `$pageview` → element click on join/newsletter → `relaunch_signup`.
- Create dashboard "SaludConTech" with funnel + `relaunch_signup` trend.
- Create alert on `relaunch_signup`.

## Files changed this session

| File | Change |
|------|--------|
| `src/layouts/Layout.astro` | Added PostHog snippet with `/ingest` proxy |
| `src/components/Newsletter.astro` | Supabase signup + PostHog identify/capture |
| `functions/ingest/[[path]].js` | New PostHog proxy |
| `docs/slack-community-digest.md` | New Slack-export digest |
| `data/facts.json` | New SCT facts |
| `HANDOFF.md` | Redacted exposed token; added continuity note |
| `CLAUDE.md` | Added standing rules/gotchas |
| `HANDOFF-2026-08-11.md` | This file |

## Backups

- **Old one-pager/scaffold:** `/home/djtl/Projects/saludcontech-site/` (private repo `turnerll/saludcontech`).
- **Cloudflare token (Pages-only, lacks DNS scope):** `/tmp/cf-pages-token.txt` on this box.
  - Token ID: `35511cd6f8974a30ef14bc9196e5d023`
  - **Must be rotated** via Cloudflare dashboard → My Profile → API Tokens.
- **opencode DB backup:** `~/.local/share/opencode/opencode.db.bak-20260810-modelfix`

## Config table

| Item | Value |
|------|-------|
| Local folder | `/home/djtl/Projects/saludcontech-website` |
| GitHub repo | `https://github.com/turnerll/saludcontech-website` |
| Domain | `saludcontech.com` |
| Cloudflare account ID | `28e227916c00920d80d094fec8c1017d` |
| Cloudflare zone ID | `505ae6adf345831cb5d0a8959acc9869` |
| Cloudflare Pages project | `saludcontech-relaunch` (not `saludcontech-website`) |
| Pages preview URL | `https://0e836df8.saludcontech-relaunch.pages.dev/` |
| PostHog org ID | `019eb357-2112-0000-ca29-74d64d5a1bff` |
| PostHog project URL | `https://us.posthog.com/project/464719` |
| PostHog public key | `phc_kXyeJqG2JKPbu3Svo2E7KkYsjZn6yFHjqxLPqBBbJ3Us` |
| Supabase project | `jcxagmhvwakkxogfyrzv` |
| Supabase URL | `https://jcxagmhvwakkxogfyrzv.supabase.co` |
| Supabase table | `public.signups` |
| Slack workspace | `T7HT7BA0H` |
| Slack channel #ligazon-stats | `C0BAHDFJG2W` |
| Slack bot | `antigravity_swarm` |
| Slack export | `/mnt/t9/docs/SLACKEXPORT/SaludConTech _ Latino Health Innovation Alliance Slack export Oct 14 2017 - Mar 1 2026/` |
| Weekly digest workflow | `turnerll/ligazon/.github/workflows/weekly-digest.yml` |

## Model / session note

This session ran on `moonshotai/kimi-k2.7-code` because OpenCode locks the model at session creation. The config (`~/.config/opencode/opencode.json`) is now set to `moonshotai/kimi-k3`. Start the next session with a fresh `opencode` command (not `occ` / `--continue`) to load k3.

## Gotchas

- **Do not save SCT work in `/home/djtl/Projects/ligazon` or `/home/djtl/Projects/saludcontech-site`.** Use only `/home/djtl/Projects/saludcontech-website`.
- **Cloudflare Pages project name is `saludcontech-relaunch`.** Wrangler/API calls must use that exact name.
- **The Pages API token was exposed publicly and lacks DNS scope.** Rotate it before relying on it; create a new token with `Zone:Read`, `DNS:Edit`, and `Cloudflare Pages:Edit` if you want full API automation.
- **COMPA PostHog config lives only in the PostHog UI; there is no GitHub copy.** It must be captured manually from the browser.
- **`occ` / `opencode --continue` resurrects the previous session model.** Always start fresh after config changes.
- **Names of real people never go in git.** The Slack digest is already paraphrased; keep it that way.
